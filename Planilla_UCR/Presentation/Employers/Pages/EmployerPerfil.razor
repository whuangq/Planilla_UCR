@page "/Employer_Perfil"
@using Application.Employers
@using Application.People
@using Domain.Employers.Entities
@using Domain.Employers.DTOs
@using Domain.People.Entities
@using Presentation.Employers.Components
@using Presentation.Core.Components
@inject NavigationManager nav
@inject IPersonService PersonService



@if(_loading) {

    <MudPaper  Height="220px" Width="100%" Outlined="false" Class="border-none pa-4" Elevation="0">
         <MudText Align="Align.Center" Typo="Typo.h6">
              <MudProgressCircular Color="Color.Secondary" Style="height:70px;width:70px;" Indeterminate="true" />
         </MudText>
       
    </MudPaper>
}else{
    @if (!empty && !_loading)
    {
        List<BreadcrumbItem> _items = new List<BreadcrumbItem>
        {
            new BreadcrumbItem("Perfil", href: "#"),
        };

        <MudBreadcrumbs Items="_items" Separator=">"></MudBreadcrumbs>



        <MudText Align="Align.Center"Typo="Typo.h6" GutterBottom="true">Mi perfil</MudText>

        <MudPaper Width="100%">
            <MudList>
                <MudListItem Icon="@Icons.Filled.Drafts" IconColor="Color.Secondary" /> 
                <MudText > Email: @employerData.Email"   </MudText>

                <MudText > </MudText>
                <MudListItem  Icon="@Icons.Filled.Photo" IconColor="Color.Secondary" />
                <MudText > Nombre: @employerData.Name @employerData.LastName1 @employerData.LastName2  </MudText>
                <MudText > </MudText>
                <MudListItem Text="Cédula: "           Icon="@Icons.Filled.Person" IconColor="Color.Secondary" />
                <MudText >@employerData.Ssn  </MudText>
                <MudText > </MudText>
                <MudListItem Text="Cuenta bancaria: "  Icon="@Icons.Filled.CreditCard" IconColor="Color.Secondary" />
                <MudText >@employerData.BankAccount  </MudText>
                <MudText > </MudText>
                <MudListItem Text="Dirección: "        Icon="@Icons.Filled.LocationOn" IconColor="Color.Secondary" />
                <MudText >@employerData.Adress  </MudText>
               <MudText > </MudText>
                <MudListItem Text="Teléfono: "         Icon="@Icons.Filled.ContactPhone" IconColor="Color.Secondary" />
                <MudText >@employerData.PhoneNumber  </MudText>
                <MudText > </MudText>
            </MudList>
            <MudDivider Vertical=true FlexItem=false/>
        </MudPaper>

          
                   
        <MudPaper  Height="220px" Width="100%" Outlined="false" Class="border-none pa-4" Elevation="0">
            <MudTable @ref="@_table" Items="@_elements" RowsPerPage="4" Hover="true" Breakpoint="Breakpoint.Sm" Loading="@_loading">
                    <ToolBarContent>
                         <MudText Align="Align.Center" Typo="Typo.h6">Mi perfil</MudText>
                        <MudSpacer />
                    </ToolBarContent>
                    <HeaderContent>
                        <MudTh>Email</MudTh>
                        <MudTh>Nombre completo</MudTh>
                        <MudTh>Cedula</MudTh>
                        <MudTh>Cuenta bancaria</MudTh>
                        <MudTh>Dirección</MudTh>
                        <MudTh>Telefono</MudTh>
                        <MudTh></MudTh>
                    </HeaderContent>
                    
                    <RowTemplate>
                        <MudTd DataLabel="Email">@context.Email</MudTd>
                        <MudTd DataLabel="Nombre">@context.Name @context.LastName1 @context.LastName2</MudTd>
                        <MudTd DataLabel="Cedula">@context.Ssn</MudTd>
                        <MudTd DataLabel="Cuenta bancaria">@context.BankAccount</MudTd>
                        <MudTd DataLabel="Dirección">@context.Adress </MudTd>
                        <MudTd DataLabel="Telefono">@context.PhoneNumber</MudTd>
                         <MudTd DataLabel="">
                        <MudSpacer></MudSpacer> 
                        </MudTd>
                    </RowTemplate>
            </MudTable>
        </MudPaper>
    }
}

@code {
    private bool _hidePosition;
    private bool _loading = true;
    private bool empty = true;
    Person personInfo = new Person("jeremy@ucr.ac.cr", "", "", "", Int32.Parse("0"), "", "", "");
    public Person employerData = new Person("jeremy@ucr.ac.cr", "Jeremy", "Vargas", "Artavia", Int32.Parse("118070615"), "BK", "AD", "PN");
    private IEnumerable<Person> _elements = new List<Person>();  
    private MudTable<Person> _table;
    

    protected override async Task OnInitializedAsync()
    {
        _elements = await PersonService.GetAllInfoEmployer(personInfo);
        if (_elements.Length() == 0){
            empty = true;

           
        }
        else{
            empty = false;
        }
        _loading = false;
    }
}
