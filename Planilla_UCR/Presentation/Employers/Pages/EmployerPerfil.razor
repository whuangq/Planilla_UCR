@page "/Employer_Perfil/{employerEmail}"
@using Application.Employers
@using Application.People
@using Domain.Employers.Entities
@using Domain.Employers.DTOs
@using Domain.People.Entities
@using Presentation.Employers.Components
@using Presentation.Core.Components
@inject NavigationManager nav
@inject IPersonService PersonService


@if(_loading) {

    <MudPaper  Height="220px" Width="100%" Outlined="false" Class="border-none pa-4" Elevation="0">
         <MudText Align="Align.Center" Typo="Typo.h6">
              <MudProgressCircular Color="Color.Secondary" Style="height:70px;width:70px;" Indeterminate="true" />
         </MudText>
       
    </MudPaper>
}else{
    @if (!empty && !_loading)
    {
        List<BreadcrumbItem> _items = new List<BreadcrumbItem>
        {
            new BreadcrumbItem("Perfil", href: "#"),
        };

        <MudBreadcrumbs Items="_items" Separator=">"></MudBreadcrumbs>

      <MudText Align="Align.Center" Typo="Typo.h6" GutterBottom="true">Mi perfil</MudText>

     <MudPaper Width="100%" align-center>
           <MudList>
                <MudListItem  Icon="@Icons.Filled.Email" IconColor="Color.Secondary" />
                 <MudDivider Vertical="true"/>
                 <MudText Align="Align.Center"> 💌 Email:  @employerData.Email </MudText>

                <MudListItem  Icon="@Icons.Filled.Photo" IconColor="Color.Secondary" />
                 <MudText Align="Align.Center"> 🙋‍♂️ Nombre: @employerData.Name @employerData.LastName1 @employerData.LastName2  </MudText>

                <MudListItem  Icon="@Icons.Filled.Person" IconColor="Color.Secondary" />
                <MudText Align="Align.Center">🆔 Cédula: @employerData.Ssn  </MudText>

                <MudListItem  Icon="@Icons.Filled.CreditCard" IconColor="Color.Secondary" />
                <MudText Align="Align.Center">💵 Cuenta bancaria: @employerData.BankAccount  </MudText>

                <MudListItem  Icon="@Icons.Filled.LocationOn" IconColor="Color.Secondary" /> 
                 <MudText Align="Align.Center">🏠 Dirección: @employerData.Adress  </MudText>

                <MudListItem  Icon="@Icons.Filled.ContactPhone" IconColor="Color.Secondary" />
                 <MudText Align="Align.Center"> 📱 Teléfono: @employerData.PhoneNumber  </MudText>
           </MudList>
    </MudPaper>

    <MudPaper Width="100%" align-center>

    <MudItem xs="2"> </MudItem>
    <MudItem xs="8" sm="6" md="4">
    <MudField Variant="Variant.Text"><MudIcon Icon="@Icons.Filled.Email" Color="@Color.Secondary" /> Email: @employerData.Email</MudField>
    </MudItem>
    <MudItem xs="2"> </MudItem>

    <MudItem xs="2"> </MudItem>
    <MudItem xs="8" sm="6" md="4">
    <MudField Variant="Variant.Text"><MudIcon Icon="@Icons.Filled.Person" Color="@Color.Secondary" /> Nombre: @employerData.Name @employerData.LastName1 @employerData.LastName2</MudField>
    </MudItem>
    <MudItem xs="2"> </MudItem>

    <MudItem xs="2"> </MudItem>
    <MudItem xs="8" sm="6" md="4">
    <MudField Variant="Variant.Text"><MudIcon Icon="@Icons.Filled.Photo" Color="@Color.Secondary" /> Cédula: @employerData.Ssn </MudField>
    </MudItem>
    <MudItem xs="2"> </MudItem>

    <MudItem xs="2"> </MudItem>
     <MudItem xs="8" sm="6" md="4">
    <MudField Variant="Variant.Text"><MudIcon Icon="@Icons.Filled.CreditCard" Color="@Color.Secondary" />  Cuenta bancaria: @employerData.BankAccount</MudField>
    </MudItem>
    <MudItem xs="2"> </MudItem>

    <MudItem xs="2"> </MudItem>
    <MudItem xs="8"  sm="6" md="4">
    <MudField Variant="Variant.Text"><MudIcon Icon="@Icons.Filled.LocationOn" Color="@Color.Secondary" /> Dirección: @employerData.Adress</MudField>
    </MudItem>
    <MudItem xs="2"> </MudItem>

    <MudItem xs="2"> </MudItem>
    <MudItem xs="8"  sm="6" md="4">
    <MudField Variant="Variant.Text"><MudIcon Icon="@Icons.Filled.ContactPhone" Color="@Color.Secondary" /> Teléfono: @employerData.PhoneNumber</MudField>
    </MudItem>
    <MudItem xs="2"> </MudItem>

    </MudPaper>
                   
        <MudPaper  Height="220px" Width="100%" Outlined="false" Class="border-none pa-4" Elevation="0">
            <MudTable @ref="@_table" Items="@_elements" RowsPerPage="4" Hover="true" Breakpoint="Breakpoint.Sm" Loading="@_loading">
                    <ToolBarContent>
                         <MudText Align="Align.Center" Typo="Typo.h6">Mi perfil</MudText>
                        <MudSpacer />
                    </ToolBarContent>
                    <HeaderContent>
                        <MudTh>Email</MudTh>
                        <MudTh>Nombre completo</MudTh>
                        <MudTh>Cedula</MudTh>
                        <MudTh>Cuenta bancaria</MudTh>
                        <MudTh>Dirección</MudTh>
                        <MudTh>Telefono</MudTh>
                        <MudTh></MudTh>
                    </HeaderContent>
                    
                    <RowTemplate>
                        <MudTd DataLabel="Email">@context.Email</MudTd>
                        <MudTd DataLabel="Nombre">@context.Name @context.LastName1 @context.LastName2</MudTd>
                        <MudTd DataLabel="Cedula">@context.Ssn</MudTd>
                        <MudTd DataLabel="Cuenta bancaria">@context.BankAccount</MudTd>
                        <MudTd DataLabel="Dirección">@context.Adress </MudTd>
                        <MudTd DataLabel="Telefono">@context.PhoneNumber</MudTd>
                         <MudTd DataLabel="">
                        <MudSpacer></MudSpacer> 
                        </MudTd>
                    </RowTemplate>
            </MudTable>
        </MudPaper>
    }
    else
    {
        <MudText Align="Align.Center" Typo="Typo.h4"> Email incorrecto</MudText>
    }
}


@code {
    private bool _hidePosition;
    private bool _loading = true;
    private bool empty = true;
    Person personInfo = new Person("jeremy@ucr.ac.cr", "", "", "", Int32.Parse("0"), "", "", "");
    public Person employerData = new Person("", "", "", "", Int32.Parse("0"), "", "", "");
    [Parameter] public string employerEmail { get; set; }
    private IEnumerable<Person> _elements = new List<Person>();  
    private MudTable<Person> _table;


    protected override async Task OnInitializedAsync()
    {
        personInfo.Email = "jeremy@ucr.ac.cr";
        _elements = await PersonService.GetAllInfoEmployer(personInfo);
        employerData = _elements.FirstOrDefault();
        if ( _elements.Length() == 0){
            empty = true;
        }
        else{
            empty = false;
        }
        _loading = false;
    }
}
